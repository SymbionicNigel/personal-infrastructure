{
    "$comment": "Permissive (matches anything). Mapped to .github/workflows/* in settings.json so redhat.vscode-yaml stops applying the GitHub Workflow schema, whose workflow_run definition trips its validator (false 'Property not allowed' / 513). github.vscode-github-actions validates workflows instead."
}
